// No TypeScript types needed — just export empty objects or JSDoc for hints

/**
 * @typedef {Object} Character
 * @property {number} id
 * @property {{ full: string }} name
 * @property {{ large: string }} image
 */

/**
 * @typedef {Object} VoiceActor
 * @property {number} id
 * @property {{ full: string }} name
 * @property {{ large: string }} image
 * @property {string} language
 */

module.exports = {};
